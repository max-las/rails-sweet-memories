<h2>Inscription</h2>

<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <%= f.input :first_name, label: "Prénom" %>
  <%= f.input :name, label: "Nom" %>
  <%= f.input :birthday, label: "Date de naissance", as: :date %>
  <%= f.input :email %>
  <%= f.input :password, label: "Mot de passe" %>
  <%= f.input :password_confirmation, label: "Confirmez le mot de passe" %>
  <%= f.button :submit, "Inscription", class: "btn-primary" %>

<% end %>

<%# Précédent code %>
<% if false %>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="form-group">
    <%= f.label :first_name, "Prénom" %><br />
    <%= f.text_field :first_name %>
  </div>

  <div class="form-group">
    <%= f.label :name, "Nom" %><br />
    <%= f.text_field :name %>
  </div>

  <div class="form-group">
    <%= f.label :birthday, "Date de naissance" %><br />
    <%= f.date_field :birthday %>
  </div>

  <div class="form-group">
    <%= f.label :email, "Email" %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="form-group">
    <%= f.label :password, "Mot de passe" %>
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> caractères minimum)</em>
    <% end %><br />
    <%= f.password_field :password, autocomplete: "new-password" %>
  </div>

  <div class="form-group">
    <%= f.label :password_confirmation, "Confirmez le mot de passe" %><br />
    <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
  </div>

  <div class="actions">
    <%= f.submit "Inscription" %>
  </div>
<% end %>

<% end %>
<%# Fin précédent code %>

<%= render "devise/shared/links" %>